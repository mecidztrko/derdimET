/** Sol menü — aktif öğe (alıcı / kesimhane). */
export const NAV_ACTIVE_PRIMARY =
  'bg-primary/10 text-primary font-semibold ring-1 ring-inset ring-primary/20 shadow-sm'

/** Sol menü — aktif öğe (satıcı, teal vurgu). */
export const NAV_ACTIVE_SECONDARY =
  'bg-secondary/10 text-secondary font-semibold ring-1 ring-inset ring-secondary/25 shadow-sm'
